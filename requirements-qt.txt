# GUI dependencies — REQUIRED to launch the application. The Qt GUI (PySide6) is the
# only interface, including in simulation mode.
#
#   pip install -r requirements-qt.txt
#   python3 main.py

PySide6>=6.6      # official Qt binding (LGPL) — GUI
matplotlib>=3.5   # test-report charts (Agg backend, plotted from the CSV)
reportlab>=4.0    # PDF report generation (pure Python, no Qt)
# The GUI's real-time charts are drawn with QPainter (no dependency).
