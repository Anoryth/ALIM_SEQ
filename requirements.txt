# Dépendances pour le mode MATÉRIEL RÉEL (pilotage SCPI / NI) uniquement.
# L'interface graphique (Qt) requiert PySide6 dans tous les cas — y compris en
# simulation : voir requirements-qt.txt. Ce fichier n'ajoute que les pilotes.
#
#   pip install -r requirements.txt

pyvisa>=1.13         # Communication SCPI avec les alimentations R&S HMP4040
pyvisa-py>=0.7       # Backend VISA pur Python (si NI-VISA non installé)
psutil>=5.9          # Requis par pyvisa-py (listing des ressources TCPIP)
pyserial>=3.5        # Backend série pour pyvisa-py (optionnel)
nidaqmx>=0.9         # Acquisition National Instruments (nécessite le driver NI-DAQmx)
