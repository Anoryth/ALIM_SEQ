# Dependencies for REAL-HARDWARE mode (SCPI / NI control) only.
# The graphical interface (Qt) requires PySide6 in all cases — including in
# simulation: see requirements-qt.txt. This file only adds the drivers.
#
#   pip install -r requirements.txt

pyvisa>=1.13         # SCPI communication with the R&S HMP4040 supplies
pyvisa-py>=0.7       # Pure-Python VISA backend (if NI-VISA is not installed)
psutil>=5.9          # Required by pyvisa-py (TCPIP resource listing)
pyserial>=3.5        # Serial backend for pyvisa-py (optional)
nidaqmx>=0.9         # National Instruments acquisition (requires the NI-DAQmx driver)
