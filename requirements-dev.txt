# Dépendances de DÉVELOPPEMENT uniquement (tests). Non requises pour exécuter
# l'application (ni en simulation, ni en matériel réel).
#
#   pip install -r requirements-dev.txt
#   python -m pytest

pytest>=8.0
pdoc>=14.0      # génère la doc API HTML depuis les docstrings (tools/build-apidoc.sh)
